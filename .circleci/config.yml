version: 3.4.5
jobs:
  build:
    docker:
      - image: matblanchard/oc_lettings:latest
        auth:
          username: matblanchard
          password: $DOCKERHUB_PASSWORD  # context / project UI env-var reference
    steps:
      - run:
          name: The First Step
          command: |
            echo 'Hello World!'
            echo 'This is the delivery pipeline'